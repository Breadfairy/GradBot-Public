# OS / editor junk
.DS_Store

# Python
__pycache__/
*.pyc
.pytest_cache/
.mplconfig/
.venv/
.cache/
.codex_tmp/

# Secrets (never commit real keys)
config.ini
**/config.ini

# Build products and local caches
build/
cache/
oracle_flags.csv
tags
tags.lock
tags.temp
tags.temp.tmp

# Private tuning inputs, data caches, and generated artifacts
inputs/profiles/
inputs/klines/
inputs/Klines/
inputs/live/
inputs/clustering/
inputs/research/
outputs/

# Keep placeholder directories if they are intentionally added later.
!.gitkeep
